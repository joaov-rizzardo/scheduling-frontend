import { ChevronDownIcon } from "./data/chevron-down-icon";
import { ConfigIcon } from "./data/config-icon";
import { LockIcon } from "./data/lock-icon";
import { LogoutIcon } from "./data/logout-icon";
import { NotificationIcon } from "./data/notification-icon";
import { UserIcon } from "./data/user-icon";

export const IconMapper = {
  logout: LogoutIcon,
  user: UserIcon,
  chevron_down: ChevronDownIcon,
  config: ConfigIcon,
  lock: LockIcon,
  notification: NotificationIcon,
};
